name: CI-pipeline
on:
  push:
    branches: main
jobs:
  say-hello:
    runs-on: ubuntu-latest

  steps:
    - name: checkout
      uses: actions/checkout@v4

      name: say-hello
      run: echo " hello github actions"

