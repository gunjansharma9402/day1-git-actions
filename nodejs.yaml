name: CI-Pipeline
on: [push]
jobs:
  say-hello:
    runs-on: ubuntu-latest

  steps: 
    - name: checkout
      uses: actions/checkout@v4
    - name: say hello
      run: echo "hello GitHub Actions"
